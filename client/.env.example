# Base URL path for the app (used by Vite at build time)
# Leave as "/" when using a custom domain on GitHub Pages
# Set to "/dot-war-game/" when deploying to https://<username>.github.io/dot-war-game/
VITE_BASE_URL=/
